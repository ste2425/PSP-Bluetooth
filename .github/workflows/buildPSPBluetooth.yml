name: Build-PSP-Bluetooth
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: pwd
        run: echo $PWD
      - name: ls
        run: ls -a
      - name: Install Dependencies
        run: ./install.sh
        working-directory: /software/scripts
